server {
    listen 80;
    
    # Serve files from /app
    location /app/ {
        alias /usr/share/nginx/html/app/;
        # Try to serve file, or fall back to index.html for SPA routing
        try_files $uri $uri/ /app/index.html;
    }
}
