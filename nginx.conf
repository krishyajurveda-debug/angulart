server {
    listen 80;
    server_name localhost;

    # Serve files directly from the html folder
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # This solves the SPA routing issue perfectly
        try_files $uri $uri/ /index.html;
    }
}
