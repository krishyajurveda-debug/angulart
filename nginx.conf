events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name localhost;

        root /usr/share/nginx/html;
        index index.html;

        # Frontend SPA
        location /app/ {
            alias /usr/share/nginx/html/app/;
            try_files $uri $uri/ /app/index.html;
        }

        location = / {
            return 301 /app/;
        }

        # GraphQL Proxy - Using http:// to avoid SSL handshake/EOF errors
        location /graphql {
            proxy_pass http://springtoo.onrender.com;
            proxy_set_header Host springtoo.onrender.com;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Accept Terms Proxy - Using http://
        location /accept-terms/ {
            proxy_pass http://springtoo.onrender.com;
            proxy_set_header Host springtoo.onrender.com;
            proxy_set_header X-Forwarded-Proto $scheme;
            add_header Content-Security-Policy "default-src 'self'; script-src 'self'; connect-src 'self' https://springtoo.onrender.com;" always;
        }
    }
}
