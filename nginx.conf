server {
    listen 80;
    server_name localhost;

    # Set root to the folder where we copied the files
    root /usr/share/nginx/html;
    index index.html;

    # SPA support: Try to find the file; if not, return index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
